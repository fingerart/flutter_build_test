name: Build android apk package

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - run: |
          mkdir -p dist
          echo "Hello" > dist/hello.txt
          ls -R dist

      - uses: jakejarvis/s3-sync-action@master
        with:
          args: --acl public-read --follow-symlinks --delete
        env:
          AWS_S3_BUCKET: 'luodoubdou-audio'
          AWS_ACCESS_KEY_ID: ${{ secrets.ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.OSS_AK_SECRET }}
          AWS_S3_ENDPOINT: 'https://aliyuncs.com'
          AWS_REGION: 'oss-cn-hongkong'
          SOURCE_DIR: 'dist'
#          DEST_DIR: 'Spellai/iOS/v1.0.0'

      # 安装Flutter环境
#      - name: Install Flutter
#        uses: subosito/flutter-action@v2.16.0
#        with:
#          channel: 'stable'

      # 安装依赖
#      - name: Install dependencies
#        run: flutter pub get

      # 构建
#      - name: Build apk
#        run: flutter build apk

      # 上传到GitHub
#      - name: Upload artifact
#        uses: actions/upload-artifact@v4
#        with:
#          name: apk
#          path: build/app/outputs/flutter-apk/app-release.apk